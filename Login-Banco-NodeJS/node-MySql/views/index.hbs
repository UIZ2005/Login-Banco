<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Banco de Occidente - Ingreso</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO"
        crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .bg-success {
            background-color: #002449 !important;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #008acc 0%, #002449 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .login-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            width: 100%;
            max-width: 400px;
            position: relative;
        }

        .login-header {
            background: linear-gradient(135deg, #008acc, #00B1EB);
            padding: 40px 30px 30px;
            text-align: center;
            color: white;
            position: relative;
        }

        .login-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse"><path d="M 10 0 L 0 0 0 10" fill="none" stroke="rgba(255,255,255,0.1)" stroke-width="0.5"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>');
            opacity: 0.3;
        }

        .bank-logo {
            font-size: 2.5rem;
            margin-bottom: 10px;
            position: relative;
            z-index: 1;
        }

        .login-title {
            font-size: 1.8rem;
            font-weight: 300;
            margin-bottom: 5px;
            position: relative;
            z-index: 1;
        }

        .login-subtitle {
            font-size: 0.9rem;
            opacity: 0.9;
            position: relative;
            z-index: 1;
        }

        /* Nav Tabs Styles */
        .nav-tabs {
            display: flex;
            background: rgba(0, 138, 204, 0.1);
            margin: 0;
            padding: 0;
            border-bottom: 2px solid #e0e6ed;
        }

        .nav-tab {
            flex: 1;
            background: none;
            border: none;
            padding: 20px 15px;
            font-size: 1rem;
            font-weight: 600;
            color: #666;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .nav-tab.active {
            color: #008acc;
            background: white;
        }

        .nav-tab.active::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(135deg, #008acc, #00B1EB);
        }

        .nav-tab:hover:not(.active) {
            background: rgba(0, 138, 204, 0.05);
            color: #008acc;
        }

        .nav-tab i {
            margin-right: 8px;
        }

        .login-form {
            padding: 40px 30px;
        }

        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: block;
        }

        .welcome-text {
            text-align: center;
            margin-bottom: 30px;
            color: #002449;
        }

        .welcome-text h2 {
            font-size: 1.4rem;
            margin-bottom: 5px;
            font-weight: 600;
        }

        .welcome-text p {
            color: #666;
            font-size: 0.9rem;
        }

        .form-group {
            margin-bottom: 25px;
            position: relative;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #002449;
            font-weight: 500;
            font-size: 0.9rem;
        }

        .input-wrapper {
            position: relative;
        }

        .form-group input {
            width: 100%;
            padding: 15px 20px 15px 50px;
            border: 2px solid #e0e6ed;
            border-radius: 12px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: #f8fafc;
        }

        .form-group input:focus {
            outline: none;
            border-color: #008acc;
            background: white;
            box-shadow: 0 0 0 3px rgba(0, 138, 204, 0.1);
        }

        .input-icon {
            position: absolute;
            left: 18px;
            top: 50%;
            transform: translateY(-50%);
            color: #008acc;
            font-size: 1.1rem;
        }

        .login-btn {
            width: 100%;
            background: linear-gradient(135deg, #008acc, #00B1EB);
            color: white;
            border: none;
            padding: 16px;
            border-radius: 12px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-top: 10px;
        }

        .login-btn:hover {
            background: linear-gradient(135deg, #002449, #008acc);
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(0, 138, 204, 0.3);
        }

        .login-btn:active {
            transform: translateY(0);
        }

        .register-btn {
            background: linear-gradient(135deg, #16a34a, #22c55e);
        }

        .register-btn:hover {
            background: linear-gradient(135deg, #15803d, #16a34a);
        }

        .message {
            margin-top: 20px;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
            font-weight: 500;
        }

        .message.error {
            background: #fee2e2;
            color: #dc2626;
            border: 1px solid #fecaca;
        }

        .message.success {
            background: #dcfce7;
            color: #16a34a;
            border: 1px solid #bbf7d0;
        }

        .footer-links {
            text-align: center;
            margin-top: 25px;
            padding-top: 20px;
            border-top: 1px solid #e5e7eb;
        }

        .footer-links a {
            color: #008acc;
            text-decoration: none;
            font-size: 0.85rem;
            margin: 0 10px;
            transition: color 0.3s ease;
        }

        .footer-links a:hover {
            color: #002449;
        }

        .security-badge {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-top: 20px;
            color: #666;
            font-size: 0.8rem;
        }

        .security-badge i {
            margin-right: 5px;
            color: #008acc;
        }

        /* Responsive */
        @media (max-width: 480px) {
            .login-container {
                margin: 10px;
                border-radius: 15px;
            }

            .login-header {
                padding: 30px 20px 25px;
            }

            .login-form {
                padding: 30px 20px;
            }

            .bank-logo {
                font-size: 2rem;
            }

            .login-title {
                font-size: 1.5rem;
            }

            .nav-tab {
                padding: 15px 10px;
                font-size: 0.9rem;
            }
        }

        /* Animaciones */
        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateX(20px);
            }

            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .login-container {
            animation: slideIn 0.6s ease-out;
        }

        .form-group {
            animation: slideIn 0.6s ease-out;
            animation-fill-mode: both;
        }

        .tab-content.active {
            animation: fadeIn 0.4s ease-out;
        }

        .form-group:nth-child(1) {
            animation-delay: 0.1s;
        }

        .form-group:nth-child(2) {
            animation-delay: 0.2s;
        }

        .form-group:nth-child(3) {
            animation-delay: 0.3s;
        }

        .form-group:nth-child(4) {
            animation-delay: 0.4s;
        }

        .form-group:nth-child(5) {
            animation-delay: 0.5s;
        }

        .login-btn {
            animation-delay: 0.6s;
        }

        .custom-toast {
            border-radius: 12px;
            overflow: hidden;
            max-width: 320px;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            transition: all 0.3s ease-in-out;
        }

        .custom-toast .toast-body {
            font-size: 0.95rem;
        }
    </style>
</head>

<body>
    <div class="login-container">
        <div class="login-header">
            <div class="bank-logo">
                <i class="fas fa-university"></i>
            </div>
            <h1 class="login-title">Banco de Occidente</h1>
            <p class="login-subtitle">Banca Digital Segura</p>
        </div>

        <!-- Navigation Tabs -->
        <div class="nav-tabs">
            <button class="nav-tab active" onclick="switchTab('login', this)">
                <i class="fas fa-sign-in-alt"></i>
                Ingresar
            </button>
            <button class="nav-tab" onclick="switchTab('register', this)">
                <i class="fas fa-user-plus"></i>
                Registrarse
            </button>
        </div>

        <div class="login-form">
            <!-- Login Tab Content -->
            <div id="login-content" class="tab-content active">
                <div class="welcome-text">
                    <h2>Bienvenido</h2>
                    <p>Ingresa tus credenciales para continuar</p>
                </div>

                <form action="/auth/login" method="POST">
                    <div class="form-group">
                        <label for="email">Correo Electrónico</label>
                        <div class="input-wrapper">
                            <i class="fas fa-envelope input-icon"></i>
                            <input type="email" id="email" name="email" placeholder="correo@ejemplo.com" required>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="password">Contraseña</label>
                        <div class="input-wrapper">
                            <i class="fas fa-lock input-icon"></i>
                            <input type="password" id="password" name="password" placeholder="Ingresa tu contraseña"
                                minlength="4" required>
                        </div>
                    </div>

                    <button type="submit" class="login-btn">
                        <i class="fas fa-sign-in-alt" style="margin-right: 8px;"></i>
                        Ingresar
                    </button>
                </form>

                {{#if message}}
                <div class="message error">
                    <i class="fas fa-exclamation-triangle" style="margin-right: 8px;"></i>
                    {{message}}
                </div>
                {{/if}}

                <div class="footer-links">
                    <a href="#"><i class="fas fa-question-circle"></i> ¿Olvidaste tu contraseña?</a>
                    <a href="#"><i class="fas fa-phone"></i> Contacto</a>
                </div>
            </div>

            <!-- Register Tab Content -->
            <div id="register-content" class="tab-content">
                <div class="welcome-text">
                    <h2>Crear Cuenta</h2>
                    <p>Completa los datos para registrar tu cuenta</p>
                </div>

                <form action="/auth/register" method="POST">
                    <div class="form-group">
                        <label for="reg-name">Nombre Completo</label>
                        <div class="input-wrapper">
                            <i class="fas fa-user input-icon"></i>
                            <input type="text" id="reg-name" name="name" placeholder="Ingresa tu nombre completo"
                                required>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="reg-email">Correo Electrónico</label>
                        <div class="input-wrapper">
                            <i class="fas fa-envelope input-icon"></i>
                            <input type="email" id="reg-email" name="email" placeholder="correo@ejemplo.com" required>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="reg-password">Contraseña</label>
                        <div class="input-wrapper">
                            <i class="fas fa-lock input-icon"></i>
                            <input type="password" id="reg-password" name="password" placeholder="Mínimo 8 caracteres"
                                minlength="4" required>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="reg-confirm-password">Confirmar Contraseña</label>
                        <div class="input-wrapper">
                            <i class="fas fa-lock input-icon"></i>
                            <input type="password" id="reg-confirm-password" name="confirmPassword"
                                placeholder="Confirma tu contraseña" minlength="4" required>
                        </div>
                    </div>
                    {{#if message1}}
                <div class="message error">
                    <i class="fas fa-exclamation-triangle" style="margin-right: 8px;"></i>
                    {{message1}}
                </div>
                {{/if}}
                    <button type="submit" class="login-btn register-btn">
                        <i class="fas fa-user-plus" style="margin-right: 8px;"></i>
                        Crear Cuenta
                    </button>
                </form>
                <div class="footer-links">
                    <a href="#"><i class="fas fa-file-contract"></i> Términos y Condiciones</a>
                    <a href="#"><i class="fas fa-shield-alt"></i> Política de Privacidad</a>
                </div>
            </div>
            <div class="security-badge">
                <i class="fas fa-shield-alt"></i>
                Conexión segura SSL 256-bit
            </div>
        </div>
    </div>
    {{#if messageRegister}}
    <div class="toast-container position-fixed bottom-0 end-0 p-3" style="z-index: 9999">
        <div class="toast show custom-toast border-0 shadow-lg animate_animated animate_fadeInUp" role="alert"
            aria-live="assertive" aria-atomic="true">
            <div class="toast-header bg-success text-white d-flex justify-content-between align-items-center rounded-top"
                style="padding: 0.75rem 1rem;">
                <strong class="me-auto">
                    <i class="bi bi-check-circle-fill me-2"></i> El usuario se ha registrado
                </strong>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="toast"
                    aria-label="Cerrar"></button>
            </div>
            <div class="toast-body bg-light text-dark rounded-bottom" style="padding: 1rem;">
                {{messageRegister}}
            </div>
        </div>
    </div>
    {{/if}}
    <script>
        function switchTab(tabName, element) {
            // Remove active class from all tabs and contents
            document.querySelectorAll('.nav-tab').forEach(tab => tab.classList.remove('active'));
            document.querySelectorAll('.tab-content').forEach(content => content.classList.remove('active'));

            // Add active class to clicked tab and corresponding content
            element.classList.add('active');
            document.getElementById(tabName + '-content').classList.add('active');
        }

        // Form validation for registration
        document.getElementById('register-content').addEventListener('submit', function (e) {
            const password = document.getElementById('reg-password').value;
            const confirmPassword = document.getElementById('reg-confirm-password').value;

            if (password !== confirmPassword) {
                e.preventDefault();
                alert('Las contraseñas no coinciden');
                return false;
            }
        });
        document.addEventListener('DOMContentLoaded', function () {
            const toastEl = document.querySelector('.toast');

            if (toastEl) {
                const toast = new bootstrap.Toast(toastEl, {
                    autohide: false
                });
                toast.show();

                // Cuando el usuario cierra el toast, se recarga la página en la pestaña "Iniciar sesión"
                toastEl.addEventListener('hidden.bs.toast', function () {
                    window.location.href = "/";
                });
            }
        });
    </script>
</body>

</html>